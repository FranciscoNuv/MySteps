namespace MySteps
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
            // BindingContext = App.MainViewModel;
        }
    }
}
